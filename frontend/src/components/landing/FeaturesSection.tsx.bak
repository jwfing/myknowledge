import {
  Search,
  GitBranch,
  Plug,
  Brain,
  Filter,
  BarChart3,
} from "lucide-react";

const features = [
  {
    icon: Search,
    title: "Vector Search",
    description:
      "Semantic similarity search across all your knowledge. Find related memories even when the wording differs.",
    color: "text-blue-400",
    bgColor: "bg-blue-400/10",
  },
  {
    icon: GitBranch,
    title: "Knowledge Graph",
    description:
      "Entity relationships track connections between projects, APIs, components, and tech decisions.",
    color: "text-emerald-400",
    bgColor: "bg-emerald-400/10",
  },
  {
    icon: Plug,
    title: "MCP Integration",
    description:
      "Native Model Context Protocol support. Works with Claude Code, Cursor, Windsurf, and any MCP-compatible agent.",
    color: "text-purple-400",
    bgColor: "bg-purple-400/10",
  },
  {
    icon: Brain,
    title: "remember_this Tool",
    description:
      "Agents proactively save important knowledge during conversations. Hot path memories get higher confidence scores.",
    color: "text-pink-400",
    bgColor: "bg-pink-400/10",
  },
  {
    icon: Filter,
    title: "Distillation Pipeline",
    description:
      "90% of conversations are noise. Our pipeline extracts only the truly valuable knowledge snippets.",
    color: "text-amber-400",
    bgColor: "bg-amber-400/10",
  },
  {
    icon: BarChart3,
    title: "Multi-Dimensional Scoring",
    description:
      "Three-signal retrieval: semantic similarity, importance weight, and recency decay for precision results.",
    color: "text-cyan-400",
    bgColor: "bg-cyan-400/10",
  },
];

export default function FeaturesSection() {
  return (
    <section id="features" className="py-24 sm:py-32">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl sm:text-4xl font-bold tracking-tight mb-4">
            Everything Your Agent Needs to Remember
          </h2>
          <p className="text-lg text-muted-foreground max-w-2xl mx-auto">
            A comprehensive knowledge management system designed specifically
            for AI development workflows.
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {features.map((feature) => (
            <div
              key={feature.title}
              className="group relative rounded-xl border border-border/50 bg-card/50 p-6 transition-all hover:border-primary/30 hover:bg-card/80 hover:shadow-lg hover:shadow-primary/5"
            >
              <div
                className={`w-10 h-10 rounded-lg ${feature.bgColor} flex items-center justify-center mb-4`}
              >
                <feature.icon className={`w-5 h-5 ${feature.color}`} />
              </div>
              <h3 className="text-lg font-semibold mb-2">{feature.title}</h3>
              <p className="text-sm text-muted-foreground leading-relaxed">
                {feature.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
